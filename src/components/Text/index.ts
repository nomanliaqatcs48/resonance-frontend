/**
 *
 * Text
 *
 */
import styled from 'styled-components';
import type { ComponentProps } from 'react';

// const CustomText = styled.p`
//   color: #141414;
//   font-size: ${(props) => `${props.size}px` || '16px'};
//   font-style: normal;
//   font-weight: ${(props) => props.weight || 500};
//   line-height: ${(props) => `${props.lineHeight}px` || 'normal'};
// `;

// interface Props extends ComponentProps<"p"> {
//   label: string;
//   size: number;
//   weight: number;
//   lineHeight: number;
// }

// function Text({ label, ...rest }: Props) {
  function Text() {
  // const { label } = props;
  // return <CustomText {...rest}>{label}</CustomText>;
}

export default Text;
